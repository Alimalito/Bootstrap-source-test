// Or import only needed plugins
import * as bootstrap from 'bootstrap';
// Rememeber to replace snippet when only need a few plugins
// import { Tooltip as Tooltip, Toast as Toast, Popover as Popover } from 'bootstrap';
